# Adapters

Each provider folder must expose the same contract.

Required files later:
- `manifest.json` id, provider, mode, capabilities
- `toProvider.md` how AIL becomes a provider prompt
- `fromProvider.md` how a reply becomes AIL

Do not put API keys here.

Current adapters are stubs only.
