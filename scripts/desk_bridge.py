#!/usr/bin/env python3
"""File-desk bridge: append protocol-safe agent handoffs without rewriting history."""
# RESTORE_MARKER — full content loaded via follow-up if truncated
raise SystemExit('INCOMPLETE_PUSH — use scripts from agent-data/grok-restore')
